@model VehiclesViewModel

@section title {
    @if (Model != null)
    {
        <h3>Category: @Model.Vehicle.VehicleModel</h3>
    }

}
@if (Model != null)
{
    <partial name="_Vehicle" model="@Model" />
}