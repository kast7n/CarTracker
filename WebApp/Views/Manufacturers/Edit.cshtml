@model Manufacturer

@section title {
    @if (Model != null)
    {
        <h3>Editing: @Model.ManufacturerName </h3>
    }

}
@if (Model != null)
{
    <partial name="_Manufacturers" model="@Model" />
}
