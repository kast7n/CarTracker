@model Maintenance

@section title {
    @if (Model != null)
    {
        <h3>Maintenace: @Model.MaintenanceType for @Model.Vehicle?.VehicleModel</h3>
    }

}
@if (Model != null)
{
    <partial name="_Maintenance" model="@Model" />
}
